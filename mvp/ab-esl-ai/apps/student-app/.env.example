# Student App Environment Configuration
# Copy this file to .env.local for local development

# Backend API URL - the student app uses Next.js rewrites
# so /api/* routes are proxied to this URL
# See next.config.js for the rewrite configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
